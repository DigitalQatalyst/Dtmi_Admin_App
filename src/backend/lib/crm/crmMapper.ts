// Placeholder mapping utilities from CRM payloads to dashboard domain types

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function mapActivationMetricsFromCRM(payload: unknown) {
  // Map CRM payload into our internal activation metrics structure
  // Implement later when CRM schema is available
  return payload;
}

export default { mapActivationMetricsFromCRM };


